name: Build Windows Exe

on:
  push:
    branches: [ main ] # Se activará cada vez que subas cambios a la rama main

jobs:
  build:
    runs-on: windows-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Set up Python
      uses: actions/setup-python@v4
      with:
        python-version: '3.10' # Cambia a tu versión de python si es otra

    - name: Install dependencies
      run: |
        python -m pip install --upgrade pip
        pip install pyinstaller
        if (Test-Path requirements.txt) { pip install -r requirements.txt }

    - name: Build with PyInstaller
      run: |
        pyinstaller --noconfirm --onefile --windowed --add-data "click.wav;." mision_numerologica_gui.py

    - name: Upload Artifact
      uses: actions/upload-artifact@v3
      with:
        name: Mision-Numerologica-Windows
        path: dist/mision_numerologica_gui.exe
